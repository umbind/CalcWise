---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCategoryMetadata, getCalculatorsByCategory } from '../../lib/calculations/registry';

const category = getCategoryMetadata('math');
if (!category) throw new Error('Category metadata for math not found');

const calculators = getCalculatorsByCategory('math');

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: category.faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: faq.answer,
    },
  })),
};

const collectionSchema = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  name: `${category.name} - CalcWise`,
  description: category.description,
  url: `https://calcwise.webcalcapps.workers.dev${category.route}/`,
  hasPart: calculators.map((calc) => ({
    '@type': 'WebApplication',
    name: calc.name,
    url: `https://calcwise.webcalcapps.workers.dev${calc.route}/`,
    applicationCategory: 'EducationalApplication',
    operatingSystem: 'All',
  })),
};
---

<BaseLayout
  title={`${category.name}: Percentage, Fraction, Statistics & Scientific Tools`}
  description={category.description}
  breadcrumbs={[{ label: category.shortName }]}
  canonicalUrl={`https://calcwise.webcalcapps.workers.dev${category.route}/`}
>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
  <script is:inline type="application/ld+json" set:html={JSON.stringify(collectionSchema)} />

  <div class="max-w-5xl mx-auto space-y-6">
    <!-- Category Hero Header -->
    <header class="border-b border-gray-100 pb-3 mb-4">
      <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-2.5">
        <div class="flex items-center space-x-2.5">
          <span class="w-8 h-8 rounded-lg bg-white border border-brand-border flex items-center justify-center text-base shadow-2xs shrink-0">
            {category.icon}
          </span>
          <div>
            <div class="flex items-center space-x-2">
              <span class="text-[10px] font-bold uppercase tracking-wider text-brand-primary">
                Category Hub
              </span>
              <span class="text-gray-300">•</span>
              <span class="inline-flex items-center text-[10px] font-semibold text-emerald-700">
                <span class="w-1.5 h-1.5 rounded-full bg-emerald-500 mr-1"></span>
                {calculators.length} Mathematical Engines
              </span>
            </div>
            <h1 class="text-base sm:text-lg font-bold text-brand-dark tracking-tight leading-tight">
              {category.name}
            </h1>
          </div>
        </div>

        <div class="flex items-center space-x-2 self-start sm:self-auto">
          <span class="inline-flex items-center px-2 py-0.5 rounded text-[10px] font-semibold bg-blue-50 text-blue-800 border border-blue-200">
            Tier T0 Deterministic
          </span>
        </div>
      </div>

      <p class="text-xs text-brand-muted mt-1.5 max-w-2xl leading-normal">
        {category.description}
      </p>
    </header>

    <!-- Calculator Grid -->
    <section aria-labelledby="tools-list-heading">
      <div class="flex justify-between items-baseline mb-3">
        <h2 id="tools-list-heading" class="text-sm font-bold text-brand-dark tracking-tight">
          Axiomatic & Statistical Calculators
        </h2>
        <span class="text-[11px] text-brand-muted font-medium">Exact Fraction & AST Parsing</span>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
        {calculators.map((calc) => (
          <a
            href={calc.route}
            title={calc.description}
            class="bg-gradient-to-br from-purple-50/80 via-white to-violet-50/50 border border-purple-200/90 rounded-xl px-3.5 py-2.5 hover:border-purple-500 hover:shadow-md hover:-translate-y-0.5 transition-all flex items-center justify-between group"
          >
            <div class="flex items-center space-x-2.5 min-w-0 pr-2">
              <span class="w-8 h-8 rounded-lg bg-purple-100 text-purple-800 flex items-center justify-center text-base shrink-0 group-hover:scale-110 transition-transform shadow-2xs">
                {calc.icon}
              </span>
              <div class="min-w-0">
                <h3 class="font-bold text-sm text-purple-950 group-hover:text-purple-700 transition truncate leading-tight">
                  {calc.name}
                </h3>
                <span class="text-[10px] font-mono text-purple-400 block truncate -mt-0.5">
                  ID: {calc.id}
                </span>
              </div>
            </div>
            <span class="px-1.5 py-0.5 text-[9px] font-extrabold rounded bg-purple-100 text-purple-700 border border-purple-200 shrink-0">
              {calc.riskTier}
            </span>
          </a>
        ))}
      </div>
    </section>

    <!-- Mathematical Principles & Standards -->
    <section class="bg-white rounded-2xl border border-brand-border p-6 sm:p-8 space-y-6 shadow-2xs">
      <div>
        <span class="text-xs font-bold uppercase tracking-wider text-brand-primary">
          Mathematical Governance & Traceability
        </span>
        <h2 class="text-xl font-black text-brand-dark tracking-tight mt-1">
          Exact Fractional Arithmetic & Sandboxed AST Parsing
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 text-xs text-brand-dark leading-relaxed">
        <div class="space-y-3">
          <h3 class="font-bold text-sm text-brand-dark">Exact Euclid GCD Fraction Reductions</h3>
          <p class="text-brand-muted">
            Rather than converting rational numbers into binary floating-point representations (which introduce repeating decimal rounding errors such as 1/3 ≈ 0.33333333333), CalcWise computes exact common denominators and reduces fractions to lowest terms using the Euclidean Greatest Common Divisor (GCD) algorithm.
          </p>
          <div class="bg-brand-surface p-3 rounded-lg border border-gray-200 font-mono text-[11px] text-brand-primary">
            a/b ± c/d = (a·d ± b·c) / (b·d) → reduced via GCD(num, den)
          </div>
        </div>

        <div class="space-y-3">
          <h3 class="font-bold text-sm text-brand-dark">Sandboxed Expression Evaluation</h3>
          <p class="text-brand-muted">
            The Scientific Calculator processes algebraic expressions through a dedicated lexical scanner and recursive-descent parser, enforcing strict PEMDAS operator precedence. We never use arbitrary JavaScript <code>eval()</code>.
          </p>
          <div class="p-3.5 bg-brand-surface rounded-xl border border-gray-200">
            <span class="font-semibold text-brand-dark block mb-1">Governing Standards:</span>
            <span class="text-brand-muted text-[11px] block">{category.standardsBody}</span>
            <span class="text-[11px] text-brand-muted mt-1 block">{category.standardsSummary}</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Category FAQs -->
    <section class="space-y-4" aria-labelledby="category-faq-heading">
      <h2 id="category-faq-heading" class="text-xl font-bold text-brand-dark tracking-tight">
        Frequently Asked Questions
      </h2>

      <div class="space-y-3">
        {category.faqs.map((faq) => (
          <div class="bg-white border border-brand-border rounded-xl p-5 shadow-2xs">
            <h3 class="font-bold text-sm text-brand-dark mb-2">
              {faq.question}
            </h3>
            <p class="text-xs text-brand-muted leading-relaxed">
              {faq.answer}
            </p>
          </div>
        ))}
      </div>
    </section>

    <!-- Cross-Category Navigation -->
    <nav class="pt-6 border-t border-gray-200 flex flex-col sm:flex-row items-center justify-between gap-4 text-xs">
      <span class="font-semibold text-brand-muted">Explore Other Calculation Disciplines:</span>
      <div class="flex flex-wrap gap-2">
        <a href="/finance" class="px-3 py-1.5 bg-white border border-brand-border rounded-lg font-medium hover:border-brand-primary hover:text-brand-primary transition">
          💰 Finance & Loans
        </a>
        <a href="/converters" class="px-3 py-1.5 bg-white border border-brand-border rounded-lg font-medium hover:border-brand-primary hover:text-brand-primary transition">
          ⇄ Unit Converters
        </a>
        <a href="/construction" class="px-3 py-1.5 bg-white border border-brand-border rounded-lg font-medium hover:border-brand-primary hover:text-brand-primary transition">
          🏗️ Construction
        </a>
        <a href="/everyday" class="px-3 py-1.5 bg-white border border-brand-border rounded-lg font-medium hover:border-brand-primary hover:text-brand-primary transition">
          📅 Everyday
        </a>
      </div>
    </nav>
  </div>
</BaseLayout>
